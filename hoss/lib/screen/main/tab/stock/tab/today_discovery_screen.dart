import 'package:flutter/material.dart';

class TodaysDiscoveryScreen extends StatelessWidget {
  const TodaysDiscoveryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 700,
      color: Colors.blue,
    );
  }
}